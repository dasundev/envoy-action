name: Test

on: ['push']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build Action
        run: npm run build
      - name: Run Envoy
        uses: ./
        with:
          story: 'deploy'